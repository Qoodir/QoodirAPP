{
  "name": "qoodir-app",
  "version": "1.0.0",
  "description": "Katalog Komik dan Pemutar Multimedia Offline",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    "build": "mkdir -p www && cp index.html www/"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
